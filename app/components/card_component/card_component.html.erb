<%= tag.div(**html_attributes) do %>
  <% if header? %>
    <header class="mb-4"><%= header %></header>
  <% end %>
  <%= content %>
  <% if footer? %>
    <footer class="mt-4 border-t border-border pt-4"><%= footer %></footer>
  <% end %>
<% end %>
