<%= tag.div(**html_attributes) do %>
  <%= tag.header(class: "mb-4") { header } if header? %>
  <%= content %>
  <%= tag.footer(class: "mt-4 border-t border-border pt-4") { footer } if footer? %>
<% end %>
