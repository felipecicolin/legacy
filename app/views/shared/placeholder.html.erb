<section class="mx-auto w-full max-w-2xl">
  <%= render CardComponent.new(variant: "outlined") do %>
    <h1 class="text-2xl font-bold text-foreground"><%= t("pages.placeholder.title") %></h1>
    <p class="mt-2 text-muted-foreground"><%= t("pages.placeholder.message") %></p>
  <% end %>
</section>
